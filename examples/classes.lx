// Class declaration
class Point {
    X = 0;
    Y = 0;
    
    fn Initialize(x, y) {
        X = x;
        Y = y;
    }
    
    fn Distance() {
        return X * X + Y * Y;
    }
}

// Using class
let p = Point;
p.Initialize(3, 4);
let dist = p.Distance();

