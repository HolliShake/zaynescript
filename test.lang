import { println, scan, parseNum } from "core:io";

const x = parseNum(scan("input> "));

println("Hello", 123, x);

var counter = 0;

fn a() {
    let x = 2;
    b();
    println(x);
}

fn b() {
    for (xx := 0; xx < x; xx = xx + 1) {
        if (xx == 3) {
            continue;
        }
        if (xx == 5) {
            break;
        }
        println("|>>", xx);
    }
    println("Done", io);
}

a();

fn fact(n) {
    if (n <= 1) return 1;
    return n * fact(n - 1);
}

const g = false;

println(g && true);

println(fact(parseNum(scan("Fact> "))));
