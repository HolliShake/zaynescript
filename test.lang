import { println, scan, parseNum } from "core:io";

const x = parseNum(scan("input> "));

println("Hello", 123, x);

var counter = 0;

fn a() {
    let x = 2;
    b();
    println(x);
}

fn b() {
    while (xx := 0; xx < x) {
        xx = xx + 1;
        println("|>>", xx);
    }
    println("Done", io);
}

a();

